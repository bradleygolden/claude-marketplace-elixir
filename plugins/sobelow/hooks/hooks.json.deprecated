{
  "_comment": "Merge SessionStart array into hooks.json to enable deprecation notice",
  "SessionStart": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "echo '{\"systemMessage\": \"⚠️ DEPRECATED: sobelow@elixir has been replaced by elixir@elixir which combines all Elixir development tools. To migrate, run these commands: /plugin install elixir@elixir then /plugin uninstall sobelow@elixir\"}'",
          "timeout": 5
        }
      ]
    }
  ]
}
